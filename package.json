{
  "name": "chromatic",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:cli": "bun run build:cli:x64-glibc && bun run build:cli:x64-musl && bun run build:cli:arm64-glibc && bun run build:cli:arm64-musl",
    "build:cli:x64-glibc": "bun build --compile --minify --target=bun-linux-x64 cli.ts --outfile dist/chromatic-x64-glibc",
    "build:cli:x64-musl": "bun build --compile --minify --target=bun-linux-x64-musl cli.ts --outfile dist/chromatic-x64-musl",
    "build:cli:arm64-glibc": "bun build --compile --minify --target=bun-linux-arm64 cli.ts --outfile dist/chromatic-arm64-glibc",
    "build:cli:arm64-musl": "bun build --compile --minify --target=bun-linux-arm64-musl cli.ts --outfile dist/chromatic-arm64-musl",
    "build:all": "bun run build && bun run build:cli",
    "check": "bun run format:check && bunx tsc --noEmit",
    "preview": "vite preview",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "solid-js": "^1.9.10"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.7.4",
    "vite": "^7.2.6",
    "vite-plugin-solid": "^2.11.10",
    "typescript": "^5.9.3"
  }
}
